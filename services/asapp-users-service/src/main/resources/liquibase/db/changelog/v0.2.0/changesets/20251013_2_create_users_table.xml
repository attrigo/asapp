<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="20251013_2-1" author="atrigo">
        <preConditions onFail="MARK_RAN" onSqlOutput="TEST">
            <not>
                <tableExists tableName="users"/>
            </not>
        </preConditions>

        <createTable tableName="users">
            <column name="id" type="uuid" defaultValueComputed="uuid_generate_v4()" remarks="The user's unique identifier">
                <constraints primaryKey="true" primaryKeyName="pk_user"/>
            </column>
            <column name="first_name" type="varchar(100)" remarks="The user's first name">
                <constraints nullable="false"/>
            </column>
            <column name="last_name" type="varchar(100)" remarks="The user's last name">
                <constraints nullable="false"/>
            </column>
            <column name="email" type="varchar(100)" remarks="The user's email">
                <constraints nullable="false" unique="true" uniqueConstraintName="uc_users_email"/>
            </column>
            <column name="phone_number" type="varchar(11)" remarks="The user's phone number">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <rollback>
            <dropTable tableName="users"/>
        </rollback>

        <comment>Creates the user table with columns id, first_name, last_name, email and phone_number</comment>
    </changeSet>

</databaseChangeLog>
