# Spring Base Application properties
spring.application.name=asapp-users-service

# Config Client properties
spring.cloud.config.password=${CONFIG_SERVER_PASSWORD}
spring.cloud.config.username=${CONFIG_SERVER_USERNAME}

# Server properties
server.port=${SERVER_PORT}
server.servlet.context-path=/asapp-users-service

# Security properties
spring.security.user.name=${SERVICE_USERNAME}
spring.security.user.password=${SERVICE_PASSWORD}

# Datasource properties
spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}

# Redis properties
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.port=${REDIS_PORT}

# Liquibase properties
spring.liquibase.change-log=liquibase/db/changelog/db.changelog-master.xml
spring.liquibase.contexts=docker
spring.liquibase.password=${spring.datasource.password}
spring.liquibase.url=${spring.datasource.url}
spring.liquibase.user=${spring.datasource.username}

# Management properties
management.server.base-path=${server.servlet.context-path}
management.server.port=${MANAGEMENT_PORT}

# Eureka Client properties
eureka.client.service-url.defaultZone=http://${DISCOVERY_USERNAME}:${DISCOVERY_PASSWORD}@${DISCOVERY_HOST}/eureka
eureka.instance.health-check-url-path=${server.servlet.context-path}/actuator/health
eureka.instance.home-page-url-path=${server.servlet.context-path}
eureka.instance.instance-id=${spring.application.name}:${server.port}
eureka.instance.prefer-ip-address=true
eureka.instance.status-page-url-path=${server.servlet.context-path}/actuator/info

# Resilience properties
## Circuit Breaker
resilience4j.circuitbreaker.instances.tasks.sliding-window-size=${TASKS_CB_SLIDING_WINDOW_SIZE}
resilience4j.circuitbreaker.instances.tasks.minimum-number-of-calls=${TASKS_CB_MINIMUM_NUMBER_OF_CALLS}
resilience4j.circuitbreaker.instances.tasks.failure-rate-threshold=${TASKS_CB_FAILURE_RATE_THRESHOLD}
resilience4j.circuitbreaker.instances.tasks.wait-duration-in-open-state=${TASKS_CB_WAIT_DURATION_IN_OPEN_STATE}
## Retry
resilience4j.retry.instances.tasks.max-attempts=${TASKS_RETRY_MAX_ATTEMPTS}
resilience4j.retry.instances.tasks.wait-duration=${TASKS_RETRY_WAIT_DURATION}

# Http Service Client properties
spring.http.clients.connect-timeout=${HTTP_CLIENT_CONNECT_TIMEOUT}
spring.http.clients.read-timeout=${HTTP_CLIENT_READ_TIMEOUT}
spring.http.serviceclient.tasks.base-url=${HTTP_CLIENT_TASKS_BASE_URL}

# Custom application properties
## Security properties
asapp.security.jwt-secret=${ASAPP_SECURITY_JWT_SECRET}
